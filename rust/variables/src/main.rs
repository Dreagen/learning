fn main() {
    let tup = ("Nick", 36);
    let (name, age) = tup;
    let age = tup.1;

    let error_codes = [200, 404, 500];
    let not_found = error_codes[1];
    let error = error_codes[3];

    let byte = [0; 8];
}
